// Configure only owner-confirmed public receiving details. Never put secrets here.
// crypto entries: {asset, network, address, memo?}; memo/tag is shown when required.
// Empty destinations keep the donation entry hidden.
export const supportConfig = {appName:'ChromeTabX', url:null, crypto:[]};
